#!/bin/sh

## Config variables for mesh injection process ##

# Preconfigured variables:
#
# - ROOT - The root of the repository
# - DEV_NAME - The device name (as implied by CONFIG -> CONFIG.devname symlink)

# A regex to match the prefixes of the Xperia Z phones to be flashed
XZ_SERIAL='^(BH)'
